version: "3.9"

services:
  app:
    container_name: vacancies_bot
    build: .
    volumes:
      - "session:nikita_avito.session./nikita_avito.session"
      - "vacancies:./vacancies.txt"

volumes:
  session:
  vacancies:
